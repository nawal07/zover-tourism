/**
 * Structured data & supplemental SEO markup.
 * Dynamic metadata is handled via Next.js `generateMetadata` + `buildPageMetadata`.
 * Extend with JSON-LD schemas when CMS content is connected.
 */
export function PageSeo() {
  return null;
}
